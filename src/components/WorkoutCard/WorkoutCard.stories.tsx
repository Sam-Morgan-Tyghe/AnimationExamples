import WorkoutCard from '.'

export default {
  component: WorkoutCard,
  title: 'components/WorkoutCard',
}

export const WorkoutCardView = () => (
  <WorkoutCard id={''} title={''} description={''} type={''} />
)
